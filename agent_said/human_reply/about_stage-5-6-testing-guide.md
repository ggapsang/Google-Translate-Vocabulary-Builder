- [ ] **복습 필요 순**: 숙련도 낮은 단어가 우선 -> 현재는 복습 기능이 없으므로 해당 테스트는 진행 불가함 (이후 단계에서 추가로 진행)

- [ ] 🗑️ 클릭 시 `"word" 삭제?` + 삭제/취소 버튼 표시 -> `Delete "word"?`

- use 와 같이 명사와 동사가 동시에 있는 경우
    - UI 개선사항. 필터 드롭다운에서 한번 클릭시 필터 적용(예 품사에서 명사). 해당 부분을(명사)를 한번더 클릭시 필터 해제되도록 기능 수정. 현재는 '전체'를 눌러야만 필터가 해제됨
    - 맨 앞에 명사-> 목록으로 표기됨
    - 중간에 뜻 리스트에서도 명사와 동사가 구분되지 않고 배열이 나오며 순번이 채번됨

- 품사 필터에서 중복 필터링이 가능하도록 개선(동사와 명사를 동시에 선택)
 
- 검색바에 저장된 단어의 일부 (예: `hel`) 입력 -> 단어 전체를 입력시에는 해당 단어만 나오지만, 일부만 입력했을때는 예문까지 검색하여 다른 단어들도 띄우던데 의도한 것인지? 만약 의도한 것이라면 예문 상관 없이 해당 단어가 그 문자열을 포함한지만 확인하도록 수정 (단 한국어 뜻에 경우 현재 작동방식을 그대로 유지)