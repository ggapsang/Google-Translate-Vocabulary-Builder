---
tags:
  - 크롬
  - 구글크롬
  - 확장프로그램
---

# 데이터 구조 상세

## 📊 전체 스토리지 구조

```
Chrome Local Storage (5MB)
├── vocabulary_words: Array<VocabularyWord>
├── vocabulary_meta: MetaData
└── vocabulary_stats: Statistics

Chrome Sync Storage (100KB)
└── settings: UserSettings
```

---

## 🗃️ 데이터 타입 정의

### VocabularyWord

**주요 단어 데이터 객체**

```typescript
interface VocabularyWord {
  // 기본 식별자
  id: string;              // "timestamp-word" 예: "1707289800000-invoke"
  word: string;            // "invoke"
  
  // 사전 정보
  definitions: Definition[];
  
  // 학습 컨텍스트
  context: Context;
  
  // 메타데이터
  metadata: Metadata;
  
  // 사용자 커스텀 (옵션)
  userNote?: string;
  tags?: string[];         // ["업무", "중요", "TOEIC"]
}
```

**JSON 예시**:
```json
{
  "id": "1707289800000-invoke",
  "word": "invoke",
  "definitions": [...],
  "context": {...},
  "metadata": {...},
  "userNote": "프로젝트 문서에서 자주 보는 단어",
  "tags": ["업무", "동사"]
}
```

---

### Definition

**품사별 정의 정보**

```typescript
interface Definition {
  pos: PartOfSpeech;       // "동사", "명사", "형용사", "부사" 등
  meanings: Meaning[];
}

type PartOfSpeech = 
  | "동사" | "명사" | "형용사" | "부사" 
  | "전치사" | "접속사" | "감탄사"
  | "verb" | "noun" | "adjective" | "adverb"
  | "preposition" | "conjunction" | "interjection";
```

**JSON 예시**:
```json
{
  "pos": "동사",
  "meanings": [
    {
      "korean": "포함하다",
      "english": "(of a situation or event) include (something) as a necessary part or result",
      "synonyms": ["require", "necessitate", "demand", "call for"],
      "example": "his transfer to another school would involve a lengthy assessment procedure"
    },
    {
      "korean": "감다",
      "english": "furl, wind, roll, involve, wreathe, wring",
      "synonyms": ["furl", "wind", "roll"],
      "example": ""
    }
  ]
}
```

---

### Meaning

**개별 의미 정보**

```typescript
interface Meaning {
  korean: string;          // 한국어 뜻
  english: string;         // 영어 정의
  synonyms: string[];      // 유의어 목록
  example: string;         // 사전에서 제공하는 예문 (있을 경우)
}
```

---

### Context

**학습 맥락 정보**

```typescript
interface Context {
  // 번역 요청했던 원문
  sourceText: string;
  
  // 번역된 텍스트
  translatedText: string;
  
  // 언어 코드 (ISO 639-1)
  sourceLanguage: LanguageCode;
  targetLanguage: LanguageCode;
  
  // 구글 번역 URL (선택)
  url?: string;
}

type LanguageCode = 
  | "en" | "ko" | "ja" | "zh" 
  | "es" | "fr" | "de" | "ru"
  // ... 기타 언어
```

**JSON 예시**:
```json
{
  "sourceText": "Of course, most indoor spaces will also fail to provide sufficient visibility of the sky for a GPS receiver to function.",
  "translatedText": "물론 대부분의 실내 공간에서는 GPS 수신기가 제대로 작동할 만큼 충분히 보이지 않습니다.",
  "sourceLanguage": "en",
  "targetLanguage": "ko",
  "url": "https://translate.google.com/?sl=en&tl=ko&text=Of%20course..."
}
```

---

### Metadata

**메타데이터 및 학습 추적**

```typescript
interface Metadata {
  // 시간 정보
  savedAt: string;         // ISO 8601 format
  lastModified?: string;
  
  // 학습 추적
  reviewCount: number;     // 복습 횟수
  lastReviewed: string | null;  // 마지막 복습 날짜
  nextReview: string | null;    // 다음 복습 예정일 (Spaced Repetition)
  
  // 숙련도 (0-5)
  masteryLevel: 0 | 1 | 2 | 3 | 4 | 5;
  
  // 테스트 기록
  testResults: TestResult[];
  
  // 통계
  viewCount?: number;      // 조회 횟수
}

interface TestResult {
  date: string;            // ISO 8601
  correct: boolean;
  testType: TestType;
  timeSpent?: number;      // 초 단위
}

type TestType = 
  | "multiple-choice"      // 객관식
  | "fill-in-blank"        // 빈칸 채우기
  | "spelling"             // 스펠링
  | "context-match";       // 문맥 매칭
```

**JSON 예시**:
```json
{
  "savedAt": "2026-02-07T10:30:00Z",
  "lastModified": "2026-02-08T09:15:00Z",
  "reviewCount": 3,
  "lastReviewed": "2026-02-10T08:00:00Z",
  "nextReview": "2026-02-14T08:00:00Z",
  "masteryLevel": 3,
  "testResults": [
    {
      "date": "2026-02-08T09:00:00Z",
      "correct": true,
      "testType": "multiple-choice",
      "timeSpent": 5
    },
    {
      "date": "2026-02-09T09:00:00Z",
      "correct": false,
      "testType": "fill-in-blank",
      "timeSpent": 12
    }
  ],
  "viewCount": 8
}
```

---

### MetaData (전체 단어장 메타데이터)

```typescript
interface VocabularyMetaData {
  version: string;         // "1.0.0"
  totalWords: number;
  lastBackup: string | null;
  lastSync: string | null;
  
  // 언어별 통계
  languagePairs: LanguagePairStats[];
  
  // 품사별 통계
  posDistribution: Record<PartOfSpeech, number>;
}

interface LanguagePairStats {
  source: LanguageCode;
  target: LanguageCode;
  count: number;
}
```

**JSON 예시**:
```json
{
  "version": "1.0.0",
  "totalWords": 150,
  "lastBackup": "2026-02-10T10:00:00Z",
  "lastSync": null,
  "languagePairs": [
    { "source": "en", "target": "ko", "count": 120 },
    { "source": "ja", "target": "ko", "count": 25 },
    { "source": "zh", "target": "ko", "count": 5 }
  ],
  "posDistribution": {
    "동사": 45,
    "명사": 60,
    "형용사": 30,
    "부사": 15
  }
}
```

---

### Statistics (학습 통계)

```typescript
interface Statistics {
  // 전체 통계
  total: {
    words: number;
    reviews: number;
    tests: number;
  };
  
  // 일별 활동
  dailyActivity: DailyActivity[];
  
  // 학습 진도
  progress: {
    level0: number;  // 처음 본 단어
    level1: number;  // 1회 복습
    level2: number;  // 2회 복습
    level3: number;  // 3회 복습
    level4: number;  // 4회 복습
    level5: number;  // 마스터
  };
  
  // 테스트 통계
  testStats: {
    totalTests: number;
    averageScore: number;
    bestScore: number;
    worstScore: number;
  };
}

interface DailyActivity {
  date: string;         // "2026-02-07"
  wordsAdded: number;
  wordsReviewed: number;
  testsCompleted: number;
}
```

**JSON 예시**:
```json
{
  "total": {
    "words": 150,
    "reviews": 320,
    "tests": 12
  },
  "dailyActivity": [
    {
      "date": "2026-02-07",
      "wordsAdded": 8,
      "wordsReviewed": 15,
      "testsCompleted": 1
    }
  ],
  "progress": {
    "level0": 17,
    "level1": 30,
    "level2": 48,
    "level3": 35,
    "level4": 15,
    "level5": 5
  },
  "testStats": {
    "totalTests": 12,
    "averageScore": 78.5,
    "bestScore": 95,
    "worstScore": 60
  }
}
```

---

### UserSettings (사용자 설정)

```typescript
interface UserSettings {
  // UI 설정
  theme: "light" | "dark" | "system";
  language: "ko" | "en" | "ja";  // 앱 언어
  
  // 기본 언어 설정
  defaultSourceLang: LanguageCode;
  defaultTargetLang: LanguageCode;
  
  // 학습 목표
  dailyGoals: {
    newWords: number;      // 일일 단어 저장 목표
    reviews: number;       // 일일 복습 목표
  };
  
  // 알림 설정
  notifications: {
    enabled: boolean;
    time: string;          // "09:00"
    days: DayOfWeek[];
  };
  
  // 테스트 설정
  testDefaults: {
    questionCount: 10 | 20 | 30;
    testType: TestType;
    difficulty: "all" | "review-needed" | "recent";
  };
  
  // 고급 설정
  advanced: {
    autoSave: boolean;
    syncEnabled: boolean;
    backupFrequency: "daily" | "weekly" | "never";
  };
}

type DayOfWeek = "mon" | "tue" | "wed" | "thu" | "fri" | "sat" | "sun";
```

**JSON 예시**:
```json
{
  "theme": "dark",
  "language": "ko",
  "defaultSourceLang": "en",
  "defaultTargetLang": "ko",
  "dailyGoals": {
    "newWords": 10,
    "reviews": 20
  },
  "notifications": {
    "enabled": true,
    "time": "09:00",
    "days": ["mon", "tue", "wed", "thu", "fri"]
  },
  "testDefaults": {
    "questionCount": 20,
    "testType": "multiple-choice",
    "difficulty": "review-needed"
  },
  "advanced": {
    "autoSave": true,
    "syncEnabled": false,
    "backupFrequency": "weekly"
  }
}
```

---

## 💾 Storage 키 구조

### Local Storage

```javascript
{
  // 주요 데이터
  "vocabulary_words": [
    { id: "...", word: "...", ... },
    { id: "...", word: "...", ... }
  ],
  
  // 메타데이터
  "vocabulary_meta": {
    version: "1.0.0",
    totalWords: 150,
    ...
  },
  
  // 통계
  "vocabulary_stats": {
    total: {...},
    dailyActivity: [...],
    ...
  },
  
  // 인덱스 (검색 최적화)
  "vocabulary_index": {
    "invoke": "1707289800000-invoke",
    "autonomous": "1707289900000-autonomous",
    ...
  }
}
```

### Sync Storage

```javascript
{
  "settings": {
    theme: "dark",
    language: "ko",
    ...
  }
}
```

---

## 🔍 데이터 조회 패턴

### 1. 전체 단어 조회
```javascript
chrome.storage.local.get('vocabulary_words', (result) => {
  const words = result.vocabulary_words || [];
});
```

### 2. 특정 단어 조회 (인덱스 사용)
```javascript
// 1. 인덱스에서 ID 조회
chrome.storage.local.get('vocabulary_index', (result) => {
  const id = result.vocabulary_index['invoke'];
  
  // 2. ID로 단어 조회
  chrome.storage.local.get('vocabulary_words', (result) => {
    const word = result.vocabulary_words.find(w => w.id === id);
  });
});
```

### 3. 필터링 (언어 쌍)
```javascript
chrome.storage.local.get('vocabulary_words', (result) => {
  const words = result.vocabulary_words.filter(w => 
    w.context.sourceLanguage === 'en' &&
    w.context.targetLanguage === 'ko'
  );
});
```

### 4. 복습 필요 단어 조회
```javascript
chrome.storage.local.get('vocabulary_words', (result) => {
  const today = new Date().toISOString().split('T')[0];
  
  const reviewNeeded = result.vocabulary_words.filter(w =>
    w.metadata.nextReview &&
    w.metadata.nextReview <= today
  );
});
```

---

## 📝 데이터 마이그레이션

### 버전 관리

```javascript
const CURRENT_VERSION = "1.0.0";

// 마이그레이션 체크
chrome.storage.local.get('vocabulary_meta', (result) => {
  const meta = result.vocabulary_meta || { version: "0.0.0" };
  
  if (meta.version !== CURRENT_VERSION) {
    migrate(meta.version, CURRENT_VERSION);
  }
});
```

### 마이그레이션 함수 예시

```javascript
function migrate(fromVersion, toVersion) {
  const migrations = {
    "0.9.0_to_1.0.0": () => {
      // tags 필드 추가
      chrome.storage.local.get('vocabulary_words', (result) => {
        const words = result.vocabulary_words.map(w => ({
          ...w,
          tags: w.tags || []
        }));
        
        chrome.storage.local.set({ vocabulary_words: words });
      });
    }
  };
  
  const key = `${fromVersion}_to_${toVersion}`;
  if (migrations[key]) {
    migrations[key]();
  }
}
```

---

## 🔒 데이터 검증

### 저장 전 검증

```typescript
function validateWord(word: VocabularyWord): boolean {
  // 필수 필드 체크
  if (!word.id || !word.word) return false;
  if (!word.definitions || word.definitions.length === 0) return false;
  if (!word.context) return false;
  
  // ID 형식 체크
  const idPattern = /^\d+-[a-zA-Z]+$/;
  if (!idPattern.test(word.id)) return false;
  
  // 언어 코드 체크
  const validLangCodes = ['en', 'ko', 'ja', 'zh', 'es', 'fr', 'de'];
  if (!validLangCodes.includes(word.context.sourceLanguage)) return false;
  if (!validLangCodes.includes(word.context.targetLanguage)) return false;
  
  return true;
}
```

---

## 📤 Export 데이터 형식

### CSV 형식

```csv
단어,품사,한국어 뜻,영어 정의,유의어,예문(원문),예문(번역),저장일시,복습횟수,숙련도
invoke,동사,포함하다,"(of a situation or event) include...","require, necessitate, demand","Of course, most indoor...","물론 대부분의...",2026-02-07T10:30:00Z,3,3
autonomous,형용사,자율적인,"having the freedom to act independently","independent, self-governing","...autonomous flying machines","...자율 비행 기계",2026-02-07T11:00:00Z,1,2
```

### Google Sheets 형식

**Sheet 1: 단어장**
| A | B | C | D | E | F | G | H | I | J |
|---|---|---|---|---|---|---|---|---|---|
| 단어 | 품사 | 뜻 | 정의 | 유의어 | 예문 | 번역 | 저장일 | 복습 | 숙련도 |
| invoke | 동사 | 포함하다 | ... | ... | ... | ... | 2026-02-07 | 3 | ⭐⭐⭐ |

---

**관련 문서**:
- [[01 프로젝트 기획안]]
- [[02 기술 스펙]]
- [[03 UIUX 설계]]
- [[04 개발 로드맵]]
